# XML Output Staging

`P57.2` creates the runtime location where the FEMIC-emitted MKRF ForestModel
XML will be written in `P57.3`.

Current boundary:

- no generated `baseMKRF.xml` is present here yet; and
- no compatibility-passthrough XML blocks have been materialized here yet.
